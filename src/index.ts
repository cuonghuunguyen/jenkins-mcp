// Placeholder entrypoint so the toolchain (tsc/build) has a valid input file.
// Full MCP bootstrap (config load, McpServer, StdioServerTransport, shebang) is
// implemented in plan 01-06 per RESEARCH.md Recommended Project Structure.
export {};
