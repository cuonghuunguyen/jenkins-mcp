node_modules/
dist/
*.log
.env
.turbo/
*.tsbuildinfo

# jenkins_log(save_to) default output directory
.jenkins-mcp/

# A literal `~` directory, created when PNPM_HOME is unexpanded during `pnpm add -g`
~/
